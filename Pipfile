[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
flask-cors = "*"

[dev-packages]
pytest = "*"
pytest-flask = "*"
pytest-cov = "*"

[requires]
python_version = "3.10"

[scripts]
test = "pytest ."
coverage = "pytest --cov-report term-missing --cov=."
