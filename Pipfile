[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
flask-cors = "*"
flask-sqlalchemy = "*"

[dev-packages]
pytest = "*"
pytest-flask = "*"
pytest-cov = "*"

[requires]
python_version = "3.9.1"

[scripts]
test = "pytest ."
coverage = "pytest --cov-report term-missing --cov=."
